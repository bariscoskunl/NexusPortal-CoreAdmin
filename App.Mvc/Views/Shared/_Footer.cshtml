<footer class="border-top footer text-muted bg-light">
    <div class="container py-5">
        <div class="row">
            <div class="col-md-4 mb-3">
                <h5 class="text-dark fw-bold">CoreAdmin</h5>
                <p class="small">
                    Gelişmiş yetkilendirme ve kullanıcı yönetim sistemi.
                    Güvenli, hızlı ve ölçeklenebilir .NET çözümleri.
                </p>
                <div class="d-flex gap-3 mt-3">
                    <a href="https://github.com/bariscoskunl" class="text-muted">Github <i class="bi bi-github"></i></a>
                    <a href="https://www.linkedin.com/in/bariscoskun441" class="text-muted">LinkedIn <i class="bi bi-linkedin"></i></a>
                </div>
            </div>

            <div class="col-md-4 mb-3">
                <h5 class="text-dark fw-bold">Hızlı Linkler</h5>
                <ul class="list-unstyled small">
                    <li class="mb-1"><a asp-controller="Home" asp-action="Index" class="text-decoration-none text-muted">Ana Sayfa</a></li>
                    <li class="mb-1"><a asp-controller="Home" asp-action="Privacy" class="text-decoration-none text-muted">Gizlilik Politikası</a></li>
                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <li><a asp-controller="Auth" asp-action="Logout" class="text-decoration-none text-danger">Güvenli Çıkış</a></li>
                    }
                </ul>
            </div>

            <div class="col-md-4 mb-3">
                <h5 class="text-dark fw-bold">Aktif Modüller</h5>
                <ul class="list-unstyled small text-muted">
                    <li class="mb-1"><i class="bi bi-calendar-check me-2"></i>Event Tracker</li>
                    <li class="mb-1"><i class="bi bi-heart-pulse me-2"></i>Veteriner Yönetimi</li>
                    <li class="mb-1"><i class="bi bi-shield-lock me-2"></i>Auth & Authorization</li>
                </ul>
            </div>
        </div>

        <hr class="my-4">

        <div class="d-flex justify-content-between align-items-center small">
            <span>&copy; 2026 - <strong>CoreAdmin</strong>. Tüm hakları saklıdır.</span>
            <span class="text-muted">Made with <i class="bi bi-code-slash text-primary"></i> by bariscoskunl</span>
        </div>
    </div>
</footer>